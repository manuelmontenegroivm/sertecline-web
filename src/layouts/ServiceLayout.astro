---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  intro: string;
}

const { title, intro } = Astro.props;
---

<BaseLayout title={title}>
  <article>
    <header>
      <h1>{title}</h1>
      <p>{intro}</p>
    </header>
    <slot />
  </article>
</BaseLayout>
