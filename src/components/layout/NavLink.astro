---
/**
 * Link de navegación principal (desktop). Distinto de <TextLink> (ui/):
 * sin subrayado, con estado aria-current para la página activa.
 */
import type { NavItem } from '../../data/navigation';
import { isCurrentNavItem } from '../../lib/utils/navigation';

interface Props {
  item: NavItem;
}

const { item } = Astro.props as Props;

// Página actual (EPIC 7 — Checkpoint 7.3). La regla vive en
// lib/utils/navigation.ts desde CP 8.2, compartida con MobileMenu para que las
// dos navegaciones no puedan marcar destinos distintos.
const isCurrent = isCurrentNavItem(Astro.url.pathname, item.href);
---

<a
  href={item.href}
  aria-current={isCurrent ? 'page' : undefined}
  class="text-ink-2 hover:text-ink aria-[current=page]:text-brand-ink text-sm font-medium transition-colors [transition-duration:var(--duration-fast)]"
>
  {item.label}
</a>
